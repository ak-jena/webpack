export declare class VirtualTarget extends HTMLElement {
    x: number;
    y: number;
    constructor(host: HTMLElement, x: number, y: number);
    updateBoundingClientRect(x: number, y: number): void;
    getBoundingClientRect(): DOMRect;
}
